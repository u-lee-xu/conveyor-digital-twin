# PLC半自动评分系统 - 配分规则文档 (2026修订版)

## 一、模块与子模块结构定义

系统严格遵循“前置通过、后续开启”的层级评测逻辑。

| 大模块 | 子模块(前置 Pos 1) | 子模块(后续 Pos 2+) | 子模块(后续 Pos 3+) |
|---------|-----------------|-------------------|-------------------|
| **1. 正启动测试** | 正启动 | 正停止 | - |
| **2. 反启动测试** | 反启动 | 反停止 | - |
| **3. 正切反测试** | 正切反-启动 | 正切反-切换 | 正切反-停止 |
| **4. 反切正测试** | 反切正-启动 | 反切正-切换 | 反切正-停止 |

---

## 二、行为处理与重试机制

系统根据失误发生的时机和性质采取不同策略：

### 1. 操作失误 (Operational Error)
*指由于用户原因导致的超时、误按、按钮卡住等（即 IO 响应不完整）。*
- **若发生在前置子模块**：执行 PLC 全局复位，直接重新开始本大模块的 Pos 1 测试。
- **若发生在后续子模块**：
    1. 执行 PLC 全局复位。
    2. **环境引导**：系统提示用户操作“前置按钮”以恢复所需的运行状态。
    3. **断点重试**：状态恢复后，系统自动重启当前后续子模块的评测。

### 2. 功能未能实现 (Functional Failure)
*指用户操作正确，但 PLC 程序逻辑未产生预期电气结果（如无自锁、无响应）。*
- **判定逻辑**：系统直接记录当前子项的实得分（或 0 分），评价为“无自锁”或“未响应”。
- **后续处理**：**判定失败并立即跳过**当前大模块的所有剩余子模块。后续子模块均记录为 0 分（评价：前置不通过）。

---

## 三、核心评分细则 (参照颗粒度评分表)

系统共分 4 个大模块，每模块 25 分，总计 100 分。各个子模块及评分项详细配置如下（后续程序开发参照此细则进行状态机和积分模块的设计）：

### 1. 正启动测试 (25分)
用于验证正转电机的启动与停止功能，重点在于验证基础的自锁及 IO 响应。

| 子模块 | 评分项 | 分数 | 评分说明 |
|---|---|---|---|
| **正启动** (前置) | 正启动按钮检测 | 2 | 首次验证正启动按钮 |
| | 正转_运行_状态变化 | 6 | 首次验证正转运行 |
| | 正转_运行_状态保持 | 5 | 首次验证正转自锁 |
| **正停止** (后续) | 停止按钮检测 | 2 | 首次验证停止按钮 |
| | 正转_停止_状态变化 | 5 | 首次验证正转停止 |
| | 正转_停止_状态保持 | 5 | 首次验证停止自锁 |

### 2. 反启动测试 (25分)
用于验证反转电机的启动与停止功能，重点在于验证基础的自锁及 IO 响应。

| 子模块 | 评分项 | 分数 | 评分说明 |
|---|---|---|---|
| **反启动** (前置) | 反启动按钮检测 | 2 | 首次验证反启动按钮 |
| | 反转_运行_状态变化 | 6 | 首次验证反转运行 |
| | 反转_运行_状态保持 | 5 | 首次验证反转自锁 |
| **反停止** (后续) | 停止按钮检测 | 2 | 首次验证停止按钮 |
| | 反转_停止_状态变化 | 5 | 首次验证反转停止 |
| | 反转_停止_状态保持 | 5 | 首次验证停止自锁 |

### 3. 正切反测试 (25分)
用于验证在正转运行状态下直接按下反转按钮时，互锁逻辑的有效性与状态切换的正确性。

| 子模块 | 评分项 | 分数 | 评分说明 |
|---|---|---|---|
| **正切反-启动** (前置) | 正启动按钮检测 | 1 | 重复步骤 |
| | 正转_运行_状态变化 | 1 | 重复步骤 |
| | 正转_运行_状态保持 | 1 | 重复步骤 |
| **正切反-切换** (后续) | 反启动按钮检测 | 1 | 重复步骤 |
| | 正转_停止_状态变化 | 5 | 正切反核心评分 |
| | 正转_停止_状态保持 | 4 | 正切反核心评分 |
| | 反转_运行_状态变化 | 5 | 正切反核心评分 |
| | 反转_运行_状态保持 | 4 | 正切反核心评分 |
| **正切反-停止** (后续) | 停止按钮检测 | 1 | 重复步骤 |
| | 反转_停止_状态变化 | 1 | 重复步骤 |
| | 反转_停止_状态保持 | 1 | 重复步骤 |

### 4. 反切正测试 (25分)
用于验证在反转运行状态下直接按下正转按钮时，互锁逻辑的有效性与状态切换的正确性。

| 子模块 | 评分项 | 分数 | 评分说明 |
|---|---|---|---|
| **反切正-启动** (前置) | 反启动按钮检测 | 1 | 重复步骤 |
| | 反转_运行_状态变化 | 1 | 重复步骤 |
| | 反转_运行_状态保持 | 1 | 重复步骤 |
| **反切正-切换** (后续) | 正启动按钮检测 | 1 | 重复步骤 |
| | 反转_停止_状态变化 | 5 | 反切正核心评分 |
| | 反转_停止_状态保持 | 4 | 反切正核心评分 |
| | 正转_运行_状态变化 | 5 | 反切正核心评分 |
| | 正转_运行_状态保持 | 4 | 反切正核心评分 |
| **反切正-停止** (后续) | 停止按钮检测 | 1 | 重复步骤 |
| | 正转_停止_状态变化 | 1 | 重复步骤 |
| | 正转_停止_状态保持 | 1 | 重复步骤 |

---

## 四、安全与重置保障
- **环境突变监控**：在等待操作前，若检测到电机状态非预期改变，系统会强制要求环境复位。
- **互锁失效判定**：若正反转输出同时为 ON，属于致命功能失效，立即终止当前模块测试并计 0 分。