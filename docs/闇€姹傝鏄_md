# 数字孪生传送带分拣系统 - 需求说明

## 项目背景

- **部署环境**：本地局域网电脑
- **限制条件**：直接使用 ModbusTCP 协议进行本地或局域网通信

---

## 功能需求

### 1. 手动操作模式

**基础控制功能：**
- 点击按钮控制气缸伸出/缩回
- 点击按钮投放物料
- 点击按钮启停传送带

**物理现象模拟：**
- 气缸运动过程中触发磁性开关状态变化
- 气缸伸出时，若前方有物料则将物料推出
- 物料在传送带上随传送带运动

---

### 2. 演示模式（自动演示）

**完整流程：**

```
投放物料 → 上料气缸推料 → 上料传感器检测 → 传送带启动 → 
色标传感器检测 → 分拣判断 → 气缸推出 → 流程结束
```

**分拣逻辑：**

| 物料颜色 | 色标传感器 | 物料传感器 | 分拣动作 |
|---------|-----------|-----------|---------|
| 黑色 | ✓ 检测到 | - | 分拣1气缸推出 |
| 蓝色 | ✗ 未检测到 | ✓ 检测到 | 分拣2气缸推出 |

---

### 3. ModbusTCP 同步模式

**适用场景**：用户拥有与数字孪生一致的实物设备（PLC、传送带、传感器、气缸、物料）

**同步要求：**

| 实物设备 | 数字孪生同步 |
|---------|-------------|
| 气缸伸出/缩回 | 3D场景气缸同步运动 |
| 传感器触发 | 3D场景传感器状态同步 |
| 磁性开关状态 | UI面板LED同步显示 |
| 传送带运行 | 3D场景传送带同步运行 |

**核心目标**：三维场景和UI面板与现场设备实时同步显示

---

### 4. ModbusTCP 仿真模式

**适用场景**：用户无实物设备，但有PLC（实际PLC或仿真软件）

**架构方案：**
```
用户PLC (ModbusTCP Server) <--> WebSocket 代理服务器 <--> 数字孪生前端
```

**功能要求：**
- PLC 控制信号 → 虚拟产线执行动作
- 虚拟产线反馈信号（传感器、磁性开关）→ PLC

---

## 技术架构

### 通信架构

```
┌─────────────────────────────────────────────────────────────┐
│                      数字孪生前端 (React)                    │
└─────────────────────────────────────────────────────────────┘
                              │
                        WebSocket (port 8081)
                              │
                              ▼
                    ┌─────────────────────────┐
                    │  WebSocket 代理服务器   │
                    │  (Node.js / modbus-serial)│
                    └─────────────────────────┘
                              │
                        ModbusTCP (port 502)
                              │
                              ▼
                    ┌─────────────────────────┐
                    │       用户 PLC           │
                    │  (127.0.0.1 / 局域网)    │
                    └─────────────────────────┘
```

---

## Modbus 地址映射 (线圈 Coil)

| 地址 | 功能 | 类型 |
|-----|------|------|
| 0 | 启动控制 | PLC 写入 / 网页读取 |
| 1 | 复位控制 | PLC 写入 / 网页读取 |
| 2 | 上料缩回限位 | 网页写入 / PLC 读取 |
| 3 | 上料伸出限位 | 网页写入 / PLC 读取 |
| 4 | 分拣1缩回限位 | 网页写入 / PLC 读取 |
| 5 | 分拣1伸出限位 | 网页写入 / PLC 读取 |
| 6 | 分拣2缩回限位 | 网页写入 / PLC 读取 |
| 7 | 分拣2伸出限位 | 网页写入 / PLC 读取 |
| 8 | 上料传感器 | 网页写入 / PLC 读取 |
| 9 | 色标传感器 | 网页写入 / PLC 读取 |
| 10 | 物料传感器 | 网页写入 / PLC 读取 |
| 100 | 上料气缸阀 | PLC 写入 / 网页读取 |
| 101 | 分拣1气缸阀 | PLC 写入 / 网页读取 |
| 102 | 分拣2气缸阀 | PLC 写入 / 网页读取 |
| 103 | 传送带运行 | PLC 写入 / 网页读取 |

---

*文档更新时间：2026-04-19*
