# PLC半自动评分系统 - 配分规则文档（传送带分拣系统版）

## 一、模块与子模块结构定义

系统遵循"前置通过、后续开启"的层级评测逻辑，共两轮运行：
- **第一轮**：复位测试 → 上料流程测试 → 黑色物料分拣
- **第二轮**：蓝色物料分拣（含重复上料验证）

| 大模块 | 子模块（前置） | 子模块（后续） | 所属轮次 |
|--------|--------------|--------------|---------|
| **1. 复位测试** | 复位触发 | 设备归位+清料 | 第一轮 |
| **2. 上料流程测试** | 上料触发 | 传送带联动 | 第一轮 |
| **3. 黑色物料分拣** | 色标检测 | 分拣1号位动作 | 第一轮 |
| **4. 蓝色物料分拣** | 第二轮上料+蓝色运输 | 分拣2号位动作 | 第二轮 |

---

## 二、硬件特性与评分原则

### 双电控电磁阀特性

所有气缸均由**双电控电磁阀**驱动：
- 伸出和缩回各由独立线圈控制
- 给出控制脉冲后，阀芯**机械自保持**，气缸保持当前状态
- PLC 程序中**无需连续输出**维持气缸位置

**评分影响：**

| 对象 | 单电控/电机 | 双电控气缸 |
|------|-----------|-----------|
| PLC 输出方式 | 持续 ON | 脉冲触发 |
| 状态保持考察 | ✅ 考察 PLC 输出是否持续 | ❌ 不考察（阀自保持） |
| 核心评分点 | 状态变化 + 状态保持 | **控制信号触发时机** + **磁性开关到位** |

> **传送带（电机）不同**：电机需持续输出维持运行，`传送带_运行_状态保持` 仍为有效评分项。

### 磁性开关作为到位唯一依据

每个气缸配有伸出端（Out）和缩回端（In）两个磁性开关：

| 变量名 | 地址 | 说明 |
|--------|------|------|
| bFeedCylinderIn | M2 | 上料气缸缩回到位 |
| bFeedCylinderOut | M3 | 上料气缸伸出到位 |
| bPush1In | M4 | 分拣1气缸缩回到位 |
| bPush1Out | M5 | 分拣1气缸伸出到位 |
| bPush2In | M6 | 分拣2气缸缩回到位 |
| bPush2Out | M7 | 分拣2气缸伸出到位 |

---

## 三、行为处理与重试机制

### 1. 操作失误（Operational Error）

*指超时、信号未触发等非程序逻辑原因导致的响应不完整。*

- **前置子模块**：全局复位，重新开始本模块前置测试。
- **后续子模块**：全局复位后引导恢复状态，从当前后续子模块断点重试。

### 2. 功能未能实现（Functional Failure）

*信号已触发但 PLC 程序未产生预期输出。*

- 当前项记 0 分，**立即跳过**本模块全部剩余子模块（均记 0 分）。

---

## 四、核心评分细则

系统共 4 个大模块，每模块 25 分，总计 100 分。

### 1. 复位测试（25分）

按下复位按钮后，3个气缸缩回控制与传送带清料启动**同时触发**。
传送带运行 5 秒后由 PLC 定时器自动停止。

**信号链：**
```
复位按钮(M1) ──→ 上料气缸缩回控制(M100) + 分拣1缩回控制(M101) + 分拣2缩回控制(M102)
                                                + 传送带运行(M103)
              ↓（各磁性开关到位）                    ↓（5秒定时器）
         气缸缩回到位确认                        传送带自动停止
```

| 子模块 | 评分项 | 分 | 评分说明 |
|---|---|---|---|
| **复位触发**（前置） | 复位按钮检测 | 2 | PLC 接收复位信号(M1) |
| | 上料气缸_缩回_控制信号 | 4 | 复位触发缩回控制输出(M100) |
| | 上料气缸_缩回_磁性开关到位 | 3 | 缩回磁性开关确认(M2) |
| **设备归位+清料**（后续） | 分拣1气缸_缩回_控制信号 | 2 | 复位触发分拣1缩回控制(M101) |
| | 分拣1气缸_缩回_磁性开关到位 | 1 | 缩回磁性开关确认(M4) |
| | 分拣2气缸_缩回_控制信号 | 2 | 复位触发分拣2缩回控制(M102) |
| | 分拣2气缸_缩回_磁性开关到位 | 1 | 缩回磁性开关确认(M6) |
| | 传送带_清料_运行_状态变化 | 3 | 复位同时传送带启动清料(M103) |
| | 传送带_清料_运行_状态保持 | 2 | 清料期间传送带持续运行 |
| | 传送带_定时停止_状态变化 | 3 | 5秒定时器到时自动停止 |
| | 传送带_定时停止_状态保持 | 2 | 确认停止且不再重启 |

> **定时器判定**：传送带超过 8 秒仍未停止，判定为定时器逻辑未实现，记 0 分并强制停止。

---

### 2. 上料流程测试（25分）

按下启动按钮，上料气缸伸出推料；**伸出磁性开关(M3)到位后**，PLC 自动输出缩回控制信号；缩回到位后物料落入传送带；上料传感器(M8)检测到物料则传送带联动启动。

**信号链：**
```
启动按钮(M0) → 伸出控制(M100) → 伸出磁性开关(M3) → 缩回控制(M100) → 缩回磁性开关(M2)
→ 上料传感器(M8) → 传送带运行(M103)
```

| 子模块 | 评分项 | 分 | 评分说明 |
|---|---|---|---|
| **上料触发**（前置） | 启动按钮检测 | 2 | PLC 接收启动信号(M0) |
| | 上料气缸_伸出_控制信号 | 3 | 启动后输出伸出控制(M100) |
| | 上料气缸_伸出_磁性开关到位 | 3 | 伸出磁性开关确认(M3) |
| | 上料气缸_缩回_控制信号 | 4 | **由伸出磁性开关触发**缩回控制(M100) |
| | 上料气缸_缩回_磁性开关到位 | 4 | 缩回磁性开关确认(M2)，物料入传送带 |
| **传送带联动**（后续） | 上料传感器_检测_触发 | 4 | 上料传感器检测到物料(M8) |
| | 传送带_运行_状态变化 | 3 | 传感器触发后传送带启动(M103) |
| | 传送带_运行_状态保持 | 2 | 传送带持续运行至分拣传感器触发 |

---

### 3. 黑色物料分拣（25分）

黑色物料到达1号位，色标传感器(M9)触发，传送带停止，分拣1气缸执行伸出推料；**伸出磁性开关(M5)到位后**，PLC 输出缩回控制，气缸归位。

**信号链：**
```
色标传感器(M9) → 传送带停止(M103) → 分拣1伸出控制(M101) → 伸出磁性开关(M5)
→ 分拣1缩回控制(M101) → 缩回磁性开关(M4)
```

| 子模块 | 评分项 | 分 | 评分说明 |
|---|---|---|---|
| **色标检测**（前置） | 色标传感器_触发_检测 | 4 | 色标传感器识别黑色物料(M9) |
| | 传送带_停止_状态变化 | 4 | 传感器触发后传送带立即停止(M103) |
| | 传送带_停止_状态保持 | 3 | 确认停止且不再重启 |
| **分拣1号位动作**（后续） | 分拣1气缸_伸出_控制信号 | 4 | 输出分拣1气缸伸出控制(M101) |
| | 分拣1气缸_伸出_磁性开关到位 | 4 | 伸出磁性开关确认(M5) |
| | 分拣1气缸_缩回_控制信号 | 3 | **由伸出磁性开关触发**缩回控制(M101) |
| | 分拣1气缸_缩回_磁性开关到位 | 3 | 缩回磁性开关确认(M4) |

---

### 4. 蓝色物料分拣（25分）

第二轮重新启动上料（重复步骤给低分）。蓝色物料经过1号位时色标传感器**不响应**（验证互锁），继续运行至2号位，物料传感器(M10)触发，传送带停止，分拣2气缸执行分拣并自动缩回。

**信号链：**
```
启动(M0) → 上料（同第一轮）→ 传送带运行(M103)
→ 色标传感器无信号(M9=0，互锁验证)
→ 物料传感器(M10) → 传送带停止(M103) → 分拣2伸出控制(M102)
→ 伸出磁性开关(M7) → 分拣2缩回控制(M102) → 缩回磁性开关(M6)
```

| 子模块 | 评分项 | 分 | 评分说明 |
|---|---|---|---|
| **第二轮上料+蓝色运输**（前置） | 启动按钮检测 | 1 | 重复步骤(M0) |
| | 上料气缸_伸出_控制信号 | 1 | 重复步骤(M100) |
| | 上料气缸_缩回_控制信号 | 1 | 重复步骤(M100) |
| | 上料传感器_检测_触发 | 1 | 重复步骤(M8) |
| | 传送带_运行_状态变化 | 1 | 重复步骤(M103) |
| | 色标传感器_未触发_验证 | 2 | 蓝色物料经过1号位，M9 保持 0（互锁验证） |
| | 物料传感器_触发_检测 | 3 | 物料传感器识别蓝色物料(M10) |
| | 传送带_停止_状态变化 | 3 | 物料传感器触发后传送带停止(M103) |
| | 传送带_停止_状态保持 | 2 | 确认停止且不再重启 |
| **分拣2号位动作**（后续） | 分拣1气缸_未动作_验证 | 1 | 确认M5 无动作，互锁正确 |
| | 分拣2气缸_伸出_控制信号 | 4 | 输出分拣2气缸伸出控制(M102) |
| | 分拣2气缸_伸出_磁性开关到位 | 3 | 伸出磁性开关确认(M7) |
| | 分拣2气缸_缩回_控制信号 | 2 | 由伸出磁性开关触发缩回控制(M102) |

---

## 五、安全与重置保障

- **气缸空推检测**：气缸在无物料就位时动作，视为逻辑错误，当前子模块功能失效，跳过后续。
- **互锁失效**：蓝色物料分拣时 M5(分拣1伸出磁性开关) 意外触发，分拣2号位后续全部记 0 分。
- **双气缸同时伸出**：M5 与 M7 同时为 ON，属致命错误，立即终止本模块，本模块计 0 分。
- **定时器缺失**：复位后传送带超过 8 秒未停止，强制停止并记 0 分。
